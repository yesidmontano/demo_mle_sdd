{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'f=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/.claude/hooks/sdd_phase_guard.py\"; if [ -f \"$f\" ]; then exec python3 \"$f\"; fi'"
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'f=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/.claude/hooks/spec_pack_guard.py\"; if [ -f \"$f\" ]; then exec python3 \"$f\"; fi'"
          }
        ]
      }
    ]
  }
}
