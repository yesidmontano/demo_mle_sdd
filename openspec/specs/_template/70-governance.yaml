model: "<nombre-del-modelo>"
tier: 2                  # 1 | 2 | 3 — ver openspec/config.yaml
rationale: "<por qué este tier>"

approvers:
  required: false
  roles: []

retention:
  receipts: "<p. ej. 5y>"
  training_data_snapshot: "<p. ej. 1y>"

explainability:
  required: false
  method: "<shap | feature_importance | none>"

rollback:
  drill_required: true
  max_time_to_revert: "<p. ej. 15m>"

audit:
  question_to_answer: "¿Por qué este modelo está en producción, contra qué datos se validó y bajo qué criterio se aprobó?"
  answerable_by: "consulta al comprobante, sin testimonio humano"
